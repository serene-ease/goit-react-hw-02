import { OptionsButton } from '../OptionsButton/OptionsButton';

export const Options = () => {
  <OptionsButton feedback={feedback} key="good" />;
  <OptionsButton feedback={feedback} key="neutral" />;
  <OptionsButton feedback={feedback} key="bad" />;
};
